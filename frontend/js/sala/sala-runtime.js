/* SIGEE Enterprise RC6.4.0.2 — Runtime único e autossuficiente da Sala de Situação 2.0 */
(function(){
'use strict';
if(window.__SIGEE_SALA_RUNTIME_6402__) return;
window.__SIGEE_SALA_RUNTIME_6402__=true;
const VERSION='RC6.4.0.3';
const txt=v=>v==null?'':String(v).trim();
const norm=v=>txt(v).normalize('NFD').replace(/[\u0300-\u036f]/g,'').toUpperCase();
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function nte(v){const s=txt(v);const m=s.match(/NTE\s*[- ]?\s*(\d{1,2})/i);if(m)return 'NTE-'+String(Number(m[1])).padStart(2,'0');if(/^\d{1,2}$/.test(s))return 'NTE-'+String(Number(s)).padStart(2,'0');return s||'SEM NTE';}
function usuario(){return window.SIGEE_AUTORIZACAO?.usuario?.()||window.usuarioLogado||window.usuarioAtual||{};}
function perfil(){return norm(usuario().perfil||usuario().role||usuario().tipo);}
function nteUsuario(){const u=usuario();return nte(window.SIGEE_ESCOPO?.nteUsuario?.(u)||window.SIGEE_ESCOPO?.nteIdUsuario?.(u)||u.nte||u.nte_nome||u.nte_id);}
function contexto(){const p=perfil();const global=p==='MASTER';return {permitido:['MASTER','GESTOR','ADMINISTRADOR'].includes(p),perfil:p,global,nte:global?null:nteUsuario(),escopo:global?'ESTADUAL':'NTE'};}
function supabase(){try{return window.obterSupabaseSIGEE?.()||window.criarClienteSupabaseSIGEE?.()||window.SIGEE_SUPABASE?.criarCliente?.()||window.supabaseClient||null;}catch(_){return null;}}
async function buscarTodos(){const cli=supabase();if(!cli){const local=Array.isArray(window.processosDB)?window.processosDB:[];return {dados:local.slice(),fonte:'MEMORIA_LOCAL'};}const lote=1000;let de=0,todos=[];while(true){const {data,error}=await cli.from('processos').select('*').range(de,de+lote-1);if(error)throw error;const arr=Array.isArray(data)?data:[];todos=todos.concat(arr);if(arr.length<lote)break;de+=lote;}return {dados:todos,fonte:'SUPABASE_PAGINADO'};}
function aplicarEscopo(dados,ctx){return ctx.global?dados.slice():dados.filter(p=>nte(p.nte||p.nte_nome||p.territorio||p.nte_id)===ctx.nte);}
async function carregarDados(){const ctx=contexto();if(!ctx.permitido)throw new Error('Perfil sem acesso à Sala de Situação 2.0.');const r=await buscarTodos();return {contexto:ctx,processos:aplicarEscopo(r.dados,ctx),totalCarregado:r.dados.length,fonte:r.fonte};}
const etapa=p=>txt(p.etapa_atual||p.etapa||p.fase_atual)||'Desarquivamento';
const responsavel=p=>txt(p.tecnico_atribuido||p.tecnico_responsavel||p.responsavel_etapa||p.analista||p.digitador||p.conferente||p.responsavel||p.usuario_lancamento||p.criado_por_nome);
function data(v){if(!v)return null;const d=new Date(v);return Number.isNaN(d.getTime())?null:d;}
function dias(v){const d=data(v);return d?Math.max(0,Math.floor((Date.now()-d.getTime())/86400000)):0;}
function finalizado(p){const e=norm(etapa(p));return e.includes('RETIR')||e.includes('INDEFER')||e.includes('DEFERIDO');}
function limite(e){const n=norm(e);if(n.includes('DESARQ'))return 30;if(n.includes('ANAL'))return 7;if(n.includes('DIGIT'))return 15;if(n.includes('CONFER'))return 10;if(n.includes('ASSIN'))return 7;return null;}
function atraso(p){const l=limite(etapa(p));return !finalizado(p)&&l!=null&&dias(p.data_etapa_atual||p.data_etapa||p.prazo_inicio||p.created_at)>l;}
function risco(p){if(finalizado(p))return 'NORMAL';const d=dias(p.data_etapa_atual||p.data_etapa||p.prazo_inicio||p.created_at),l=limite(etapa(p)),pr=norm(p.prioridade);if(atraso(p)&&d>(l||0)+15)return 'CRITICO';if(atraso(p)||pr.includes('URG')||pr.includes('CRIT'))return 'ALTO';if((l!=null&&d>=Math.max(0,l-3))||!responsavel(p))return 'MEDIO';return 'NORMAL';}
function score(m){if(!m.ativos)return 0;return Math.min(100,Math.round((m.criticos/m.ativos*100)*.5+(m.atrasados/m.ativos*100)*.3+(m.semResponsavel/m.ativos*100)*.2));}
function classe(s){return s>=76?'CRITICO':s>=51?'ALTO':s>=26?'ATENCAO':'NORMAL';}
function analisar(processos){const ativos=processos.filter(p=>!finalizado(p)),finalizados=processos.filter(finalizado),hoje=new Date().toDateString();const recebidosHoje=processos.filter(p=>data(p.created_at||p.data_solicitacao)?.toDateString()===hoje).length;const concluidosHoje=finalizados.filter(p=>data(p.finalizado_em||p.retirado_em||p.updated_at)?.toDateString()===hoje).length;const atrasados=ativos.filter(atraso),criticos=ativos.filter(p=>risco(p)==='CRITICO'),sem=ativos.filter(p=>!responsavel(p));const etapas={};ativos.forEach(p=>{const e=etapa(p);etapas[e]=(etapas[e]||0)+1;});const mapa=new Map();for(let i=1;i<=27;i++)mapa.set('NTE-'+String(i).padStart(2,'0'),[]);processos.forEach(p=>{const k=nte(p.nte||p.nte_nome||p.territorio||p.nte_id);if(!mapa.has(k))mapa.set(k,[]);mapa.get(k).push(p);});const territorios=[...mapa].map(([codigo,ps])=>{const a=ps.filter(p=>!finalizado(p));const m={codigo,ativos:a.length,total:ps.length,criticos:a.filter(p=>risco(p)==='CRITICO').length,altos:a.filter(p=>risco(p)==='ALTO').length,atrasados:a.filter(atraso).length,semResponsavel:a.filter(p=>!responsavel(p)).length,processos:a};m.indice=score(m);m.classe=classe(m.indice);return m;}).sort((a,b)=>b.indice-a.indice||b.criticos-a.criticos||b.ativos-a.ativos);const peso={CRITICO:4,ALTO:3,MEDIO:2,NORMAL:1};const fila=ativos.slice().sort((a,b)=>peso[risco(b)]-peso[risco(a)]||dias(b.data_etapa_atual||b.created_at)-dias(a.data_etapa_atual||a.created_at));return {processos,ativos,finalizados,recebidosHoje,concluidosHoje,atrasados,criticos,semResponsavel:sem,etapas,territorios,fila,risco,etapa,responsavel,atraso};}
let state={payload:null,analise:null,active:false,rendering:false};
function section(){return document.getElementById('aba-sala-situacao');}
function shell(){const s=section();if(!s)throw new Error('Área da Sala de Situação não localizada.');state.rendering=true;s.classList.remove('hidden');s.innerHTML=`<div class="sala2-root"><header class="sala2-hero"><div><small>CENTRO DE OPERAÇÕES</small><h1>Sala de Situação 2.0</h1><p>Monitoramento territorial, alertas e resposta rápida da operação.</p></div><div class="sala2-actions"><span id="sala2-sync">Carregando...</span><button id="sala2-refresh">↻ Atualizar</button><button id="sala2-full">⛶ Tela cheia</button></div></header><div id="sala2-content" class="sala2-content"><div class="sala2-loading">Consolidando a base completa...</div></div></div>`;state.rendering=false;s.querySelector('#sala2-refresh').onclick=abrir;s.querySelector('#sala2-full').onclick=()=>!document.fullscreenElement?s.requestFullscreen?.():document.exitFullscreen?.();return s;}
function kpi(t,v,s,cls=''){return `<button class="sala2-kpi ${cls}" data-kpi="${esc(t)}"><span>${esc(t)}</span><strong>${esc(v)}</strong><small>${esc(s)}</small></button>`;}
function prepararProntuario(p){if(!p)return;window.processosDB=Array.isArray(window.processosDB)?window.processosDB:[];const i=window.processosDB.findIndex(x=>String(x.id)===String(p.id));if(i>=0)window.processosDB[i]={...window.processosDB[i],...p};else window.processosDB.push(p);window.abrirProntuarioSIGEE?.(p.id);}
function modal(titulo,arr){document.getElementById('sala2-modal')?.remove();const o=document.createElement('div');o.id='sala2-modal';o.className='sala2-modal';o.innerHTML=`<div class="sala2-modal-box"><header><div><small>DETALHAMENTO OPERACIONAL</small><h2>${esc(titulo)}</h2><p>${arr.length} registro(s)</p></div><button data-close>×</button></header><div class="sala2-table"><table><thead><tr><th>Processo</th><th>Aluno / Escola</th><th>NTE</th><th>Etapa</th><th>Responsável</th><th>Risco</th></tr></thead><tbody>${arr.map(p=>`<tr><td><button class="sala2-proc" data-id="${esc(p.id)}">${esc(p.codigo_sigee||p.codigo||p.id)}</button></td><td><b>${esc(p.aluno_nome||p.aluno||'')}</b><small>${esc(p.escola_nome||p.escola||'')}</small></td><td>${esc(nte(p.nte||p.nte_nome||p.nte_id))}</td><td>${esc(state.analise.etapa(p))}</td><td>${esc(state.analise.responsavel(p)||'Aguardando atribuição')}</td><td>${esc(state.analise.risco(p))}</td></tr>`).join('')}</tbody></table></div></div>`;document.body.appendChild(o);o.querySelector('[data-close]').onclick=()=>o.remove();o.onclick=e=>{if(e.target===o)o.remove();};o.querySelectorAll('.sala2-proc').forEach(b=>b.onclick=()=>prepararProntuario(state.analise.processos.find(x=>String(x.id)===b.dataset.id)));}
function render(){state.rendering=true;const a=state.analise,p=state.payload,c=document.getElementById('sala2-content');if(!c)return;const sla=a.ativos.length?Math.round((a.ativos.length-a.atrasados.length)*100/a.ativos.length):100;const terr=a.territorios.filter(x=>x.ativos).slice(0,12),etapas=Object.entries(a.etapas).sort((x,y)=>y[1]-x[1]);c.innerHTML=`<section class="sala2-context"><span>Escopo: <b>${p.contexto.global?'Estado da Bahia':esc(p.contexto.nte)}</b></span><span>Fonte: <b>${esc(p.fonte)}</b></span><span>Base analisada: <b>${a.processos.length}</b></span></section><section class="sala2-kpis">${kpi('Processos ativos',a.ativos.length,'Em tramitação')}${kpi('Em atraso',a.atrasados.length,'Exigem atenção','danger')}${kpi('Críticos',a.criticos.length,'Prioridade imediata','danger')}${kpi('Aguardando atribuição',a.semResponsavel.length,'Sem responsável','warn')}${kpi('SLA operacional',sla+'%','Dentro do prazo')}${kpi('Recebidos hoje',a.recebidosHoje,'Novas solicitações')}</section><section class="sala2-grid"><article class="sala2-panel sala2-flow"><header><div><small>FLUXO OPERACIONAL</small><h2>Distribuição por etapa</h2></div><span>${a.ativos.length} ativos</span></header><div class="sala2-stage-grid">${etapas.map(([e,n])=>`<button data-etapa="${esc(e)}"><span>${esc(e)}</span><strong>${n}</strong></button>`).join('')}</div></article><article class="sala2-panel sala2-alerts"><header><div><small>ALERTAS PRIORITÁRIOS</small><h2>Atenção imediata</h2></div></header><div>${a.criticos.length?`<button data-alert="criticos"><b>${a.criticos.length} processos críticos</b><span>Abrir fila prioritária</span></button>`:''}${a.atrasados.length?`<button data-alert="atrasados"><b>${a.atrasados.length} processos em atraso</b><span>Revisar prazos vencidos</span></button>`:''}${a.semResponsavel.length?`<button data-alert="sem"><b>${a.semResponsavel.length} aguardando atribuição</b><span>Equilibrar distribuição</span></button>`:''}</div></article></section><section class="sala2-grid sala2-bottom"><article class="sala2-panel sala2-territory"><header><div><small>VISÃO TERRITORIAL</small><h2>Situação dos NTEs</h2></div><span>Índice de risco 0–100</span></header><div class="sala2-legend"><i class="normal"></i>Normal <i class="atencao"></i>Atenção <i class="alto"></i>Alto <i class="critico"></i>Crítico</div><div class="sala2-territory-grid">${terr.map(t=>`<button class="${t.classe.toLowerCase()}" data-nte="${t.codigo}"><span>${t.codigo}</span><strong>${t.indice}</strong><small>${t.criticos} críticos · ${t.ativos} ativos · ${t.semResponsavel} sem responsável</small></button>`).join('')}</div></article><article class="sala2-panel sala2-queue"><header><div><small>FILA PRIORITÁRIA</small><h2>Próximas ações</h2></div><button data-fila>Ver fila completa</button></header><div>${a.fila.slice(0,8).map((x,i)=>`<button data-id="${esc(x.id)}"><b>${i+1}</b><span><strong>${esc(x.codigo_sigee||x.id)}</strong><small>${esc(x.aluno_nome||'')} · ${esc(nte(x.nte||x.nte_id))} · ${esc(a.etapa(x))}</small></span><em>${esc(a.risco(x))}</em></button>`).join('')}</div></article></section>`;
document.getElementById('sala2-sync').textContent='● Dados sincronizados • '+new Date().toLocaleTimeString('pt-BR');
c.querySelectorAll('[data-etapa]').forEach(b=>b.onclick=()=>modal(b.dataset.etapa,a.ativos.filter(p=>a.etapa(p)===b.dataset.etapa)));
c.querySelector('[data-alert="criticos"]')?.addEventListener('click',()=>modal('Processos críticos',a.criticos));c.querySelector('[data-alert="atrasados"]')?.addEventListener('click',()=>modal('Processos em atraso',a.atrasados));c.querySelector('[data-alert="sem"]')?.addEventListener('click',()=>modal('Aguardando atribuição',a.semResponsavel));
c.querySelectorAll('[data-nte]').forEach(b=>b.onclick=()=>modal('Situação '+b.dataset.nte,a.territorios.find(x=>x.codigo===b.dataset.nte)?.processos||[]));c.querySelector('[data-fila]')?.addEventListener('click',()=>modal('Fila prioritária',a.fila));c.querySelectorAll('.sala2-queue [data-id]').forEach(b=>b.onclick=()=>prepararProntuario(a.fila.find(x=>String(x.id)===b.dataset.id)));
state.rendering=false;const map={'Processos ativos':a.ativos,'Em atraso':a.atrasados,'Críticos':a.criticos,'Aguardando atribuição':a.semResponsavel};c.querySelectorAll('[data-kpi]').forEach(b=>{const arr=map[b.dataset.kpi];if(arr)b.onclick=()=>modal(b.dataset.kpi,arr);});}
async function abrir(){state.active=true;document.body.classList.add('sigee-sala2-ativa');shell();try{const payload=await carregarDados();state.payload=payload;state.analise=analisar(payload.processos);render();console.info('[SIGEE Sala 2.0 RC6.4.0.2]',{totalCarregado:payload.totalCarregado,totalNoEscopo:payload.processos.length,fonte:payload.fonte});}catch(e){console.error('[SIGEE Sala 2.0]',e);const c=document.getElementById('sala2-content');if(c)c.innerHTML='<div class="sala2-error"><strong>Falha ao carregar a Sala de Situação 2.0.</strong><br><small>'+esc(e.message)+'</small><br><button id="sala2-retry">Tentar novamente</button></div>';document.getElementById('sala2-retry')?.addEventListener('click',abrir);}}
window.SIGEE_SALA_DATA={version:VERSION,carregar:carregarDados,nte,contexto};
window.SIGEE_SALA_ENGINE={version:VERSION,analisar};
function restaurarSeNecessario(){if(!state.active||state.rendering)return;const s=section();if(!s)return;const root=s.querySelector('.sala2-root');if(s.classList.contains('hidden')||!root){console.warn('[SIGEE Sala 2.0] Conteúdo removido por outro módulo; restaurando a tela.');s.classList.remove('hidden');if(state.analise&&state.payload){shell();render();}else abrir();}}
let restoreTimer=null;const observer=new MutationObserver(()=>{if(!state.active)return;clearTimeout(restoreTimer);restoreTimer=setTimeout(restaurarSeNecessario,80);});observer.observe(document.body,{subtree:true,childList:true,attributes:true,attributeFilter:['class']});
function desativar(){state.active=false;document.body.classList.remove('sigee-sala2-ativa');}
window.SIGEE_SALA_2={version:VERSION,abrir,atualizar:abrir,restaurar:restaurarSeNecessario,desativar};
console.info('[SIGEE RC6.4.0.3] Runtime protegido da Sala de Situação 2.0 pronto.');
})();
